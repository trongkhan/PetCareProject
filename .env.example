# Copy to .env. These EXPO_PUBLIC_* values are embedded in the app bundle.
# The Supabase anon key is public by design (protected by Row-Level Security).
# NEVER put the Gemini key here — it lives only in the backend secrets.
EXPO_PUBLIC_SUPABASE_URL=https://ppuinennusunlqnttajj.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
